mind-the-gap / prepare / share
======================

To set up the share drive, plug in your external drive and run `./build.sh BLOCK_DEV`, where BLOCK_DEV is the root device (like /dev/sdb) of the drive.

